import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.unike.loja",
  appName: "UNIKE Loja",
  webDir: "dist",
  server: { androidScheme: "https" }
};

export default config;
